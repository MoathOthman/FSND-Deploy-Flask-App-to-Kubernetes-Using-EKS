apiVersion: v1
kind: Service
metadata:
  name: lesson4-project
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: lesson4-project
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lesson4-project
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 2
      maxSurge: 2
  selector:
    matchLabels:
      app: lesson4-project
  template:
    metadata:
      labels:
        app: lesson4-project
    spec:
      containers:
        - name: lesson4-project
          image: CONTAINER_IMAGE
          securityContext:
            privileged: false
            readOnlyRootFilesystem: false
            allowPrivilegeEscalation: false
          ports:
            - containerPort: 8080
